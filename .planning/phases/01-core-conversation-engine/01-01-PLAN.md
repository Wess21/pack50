---
phase: 01-core-conversation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/db/schema.sql
  - src/db/client.ts
  - src/config/env.ts
  - src/bot/index.ts
  - docker-compose.dev.yml
  - .env.example
autonomous: true
requirements:
  - BOT-01
  - BOT-03
  - CONV-02
  - CONV-07
  - DATA-02

must_haves:
  truths:
    - "Bot can connect to Telegram API and receive updates"
    - "PostgreSQL database stores users and conversation messages"
    - "Redis stores session data with 24-hour TTL"
    - "Development environment runs via docker-compose up"
    - "Environment variables configure all services securely"
  artifacts:
    - path: "src/bot/index.ts"
      provides: "grammY bot initialization and startup"
      min_lines: 40
    - path: "src/db/schema.sql"
      provides: "Database tables (users, conversations, messages)"
      contains: "CREATE TABLE users"
    - path: "src/db/client.ts"
      provides: "PostgreSQL connection pool"
      exports: ["db"]
    - path: "src/config/env.ts"
      provides: "Environment validation with Zod"
      exports: ["env"]
    - path: "docker-compose.dev.yml"
      provides: "PostgreSQL and Redis services"
      contains: "services:"
  key_links:
    - from: "src/bot/index.ts"
      to: "Telegram API"
      via: "Bot token authentication"
      pattern: "new Bot\\(env\\.BOT_TOKEN\\)"
    - from: "src/db/client.ts"
      to: "PostgreSQL"
      via: "pg Pool connection"
      pattern: "new Pool\\("
    - from: "docker-compose.dev.yml"
      to: "Redis"
      via: "ioredis connection"
      pattern: "redis:.*6379"
---

<objective>
Establish foundational infrastructure for Telegram bot with persistent data storage and session management.

Purpose: Create the architectural skeleton (bot framework + database + Redis) that all conversation features will build upon. Validates that bot can connect to Telegram, store data in PostgreSQL, and manage sessions in Redis.

Output: Working development environment where bot can start, connect to services, and respond to basic updates (though conversation logic comes in later plans).
</objective>

<execution_context>
@/Users/dmtriy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dmtriy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-conversation-engine/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Node.js project with TypeScript and dependencies</name>
  <files>
    package.json
    tsconfig.json
    .gitignore
    .env.example
  </files>
  <action>
    Create Node.js project with TypeScript configuration:

    - Initialize package.json with name "pack50-bot", type "module", engines "node >=20"
    - Install dependencies: grammy @grammyjs/conversations @grammyjs/storage-redis @grammyjs/menu ioredis pg @anthropic-ai/sdk express zod dotenv winston helmet express-rate-limit
    - Install dev dependencies: @types/node @types/express typescript tsx nodemon @types/pg
    - Configure tsconfig.json: ES2022 target, ESNext module, strict mode, outDir "dist", rootDir "src"
    - Add scripts: "dev": "nodemon --exec tsx src/index.ts", "build": "tsc", "start": "node dist/index.js"
    - Create .gitignore with node_modules, dist, .env, *.log
    - Create .env.example with placeholder variables: BOT_TOKEN, DATABASE_URL, REDIS_URL, ANTHROPIC_API_KEY, NODE_ENV

    Use grammY (not Telegraf) per research recommendation for better TypeScript support and conversation plugin.
  </action>
  <verify>
    npm install runs without errors
    tsc --noEmit validates TypeScript config
    ls package.json tsconfig.json .gitignore .env.example confirms files exist
  </verify>
  <done>
    package.json contains all required dependencies with correct versions
    TypeScript compiles successfully with strict mode enabled
    .env.example documents all required environment variables
  </done>
</task>

<task type="auto">
  <name>Task 2: Create database schema and connection client</name>
  <files>
    src/db/schema.sql
    src/db/client.ts
    src/config/env.ts
  </files>
  <action>
    Implement PostgreSQL database layer with schema from research:

    **src/config/env.ts:**
    - Use Zod to validate environment variables (BOT_TOKEN, DATABASE_URL, REDIS_URL, ANTHROPIC_API_KEY, NODE_ENV, PORT)
    - Export validated env object with types
    - Throw descriptive errors for missing required variables

    **src/db/schema.sql:**
    - Create users table: id SERIAL PRIMARY KEY, telegram_id BIGINT UNIQUE NOT NULL, username VARCHAR(255), first_name VARCHAR(255), last_name VARCHAR(255), created_at TIMESTAMP DEFAULT NOW(), updated_at TIMESTAMP DEFAULT NOW()
    - Create conversations table: id SERIAL PRIMARY KEY, user_id INTEGER REFERENCES users(id) ON DELETE CASCADE, started_at TIMESTAMP DEFAULT NOW(), ended_at TIMESTAMP, status VARCHAR(50) DEFAULT 'active', lead_data JSONB, created_at TIMESTAMP DEFAULT NOW(), updated_at TIMESTAMP DEFAULT NOW()
    - Create messages table: id SERIAL PRIMARY KEY, conversation_id INTEGER REFERENCES conversations(id) ON DELETE CASCADE, role VARCHAR(20) NOT NULL, content TEXT NOT NULL, metadata JSONB, created_at TIMESTAMP DEFAULT NOW()
    - Create leads table: id SERIAL PRIMARY KEY, conversation_id INTEGER REFERENCES conversations(id) ON DELETE CASCADE, name VARCHAR(255) NOT NULL, email VARCHAR(255) NOT NULL, phone VARCHAR(50) NOT NULL, additional_info JSONB, confirmed BOOLEAN DEFAULT FALSE, created_at TIMESTAMP DEFAULT NOW(), updated_at TIMESTAMP DEFAULT NOW()
    - Add indexes: idx_users_telegram_id, idx_conversations_user_id, idx_conversations_status, idx_messages_conversation_id, idx_leads_email
    - Create update_updated_at_column() trigger function
    - Apply triggers to users, conversations, leads tables

    **src/db/client.ts:**
    - Create PostgreSQL Pool with connection from env.DATABASE_URL
    - Export db instance
    - Add connection error handling with descriptive logging
    - Implement initDatabase() function that reads schema.sql and executes (idempotent - use CREATE IF NOT EXISTS or check existence)

    Follow research schema exactly (Architecture Patterns > Pattern 5).
  </action>
  <verify>
    tsc compiles src/db/client.ts and src/config/env.ts without errors
    cat src/db/schema.sql | grep "CREATE TABLE users" confirms schema structure
    node -e "import('./dist/config/env.js')" validates Zod schema (with valid .env)
  </verify>
  <done>
    Database schema includes all four tables (users, conversations, messages, leads) with proper foreign keys
    Environment validation throws clear errors for missing variables
    Database client exports typed Pool instance ready for queries
  </done>
</task>

<task type="auto">
  <name>Task 3: Set up development environment with Docker Compose</name>
  <files>
    docker-compose.dev.yml
    src/bot/index.ts
    src/index.ts
  </files>
  <action>
    Create local development environment with containerized services:

    **docker-compose.dev.yml:**
    - PostgreSQL service: image postgres:16-alpine, environment POSTGRES_DB=pack50, POSTGRES_USER=pack50, POSTGRES_PASSWORD=dev_password, ports 5432:5432, volume postgres_data
    - Redis service: image redis:7-alpine, ports 6379:6379, volume redis_data, command "redis-server --requirepass dev_password"
    - Add volumes: postgres_data, redis_data
    - Network: default bridge network (services auto-discover via service names)

    **src/bot/index.ts:**
    - Import Bot from grammy, env from config
    - Create bot instance: new Bot(env.BOT_TOKEN)
    - Add basic command handler: bot.command("start", (ctx) => ctx.reply("Bot is running!"))
    - Export bot instance
    - Add bot.start() with long polling for development (webhook in later phases)
    - Add graceful shutdown: process.once("SIGINT", () => bot.stop()), process.once("SIGTERM", () => bot.stop())

    **src/index.ts:**
    - Import bot from bot/index, initDatabase from db/client
    - Call initDatabase() to apply schema
    - Start bot with error handling
    - Log startup message with environment info

    Use long polling (not webhook) for development per research (BOT-03: webhook for production, long polling for dev).
  </action>
  <verify>
    docker-compose -f docker-compose.dev.yml up -d starts PostgreSQL and Redis
    docker-compose -f docker-compose.dev.yml ps shows both services running
    npm run dev starts bot without errors (requires valid BOT_TOKEN in .env)
    Send /start to bot via Telegram and receive "Bot is running!" response
  </verify>
  <done>
    Docker Compose brings up PostgreSQL and Redis services successfully
    Bot connects to Telegram API and responds to /start command
    Database schema is applied automatically on startup
    Development environment documented in .env.example
  </done>
</task>

</tasks>

<verification>
Run comprehensive integration check:

1. Start services: docker-compose -f docker-compose.dev.yml up -d
2. Verify database: docker exec -it pack50-postgres psql -U pack50 -d pack50 -c "\dt" shows users, conversations, messages, leads tables
3. Verify Redis: docker exec -it pack50-redis redis-cli -a dev_password PING returns PONG
4. Start bot: npm run dev
5. Test bot: Send /start message to bot via Telegram, verify "Bot is running!" response received
6. Check logs: Bot logs show successful connection, no errors
7. Verify environment: All required variables documented in .env.example

Success indicators:
- All services start without errors
- Database tables created with correct schema
- Bot responds to Telegram messages
- No connection errors in logs
</verification>

<success_criteria>
- Docker Compose successfully starts PostgreSQL 16 and Redis 7
- Database schema creates 4 tables (users, conversations, messages, leads) with indexes and triggers
- grammY bot connects to Telegram API using BOT_TOKEN
- Bot responds to /start command via long polling
- Environment variables validated by Zod with clear error messages
- Development workflow: docker-compose up && npm run dev starts entire stack
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-conversation-engine/01-01-SUMMARY.md` documenting:
- Infrastructure components created (bot, database, Redis)
- Database schema structure (tables, relationships, indexes)
- Development environment setup (docker-compose, npm scripts)
- Connection patterns (bot to Telegram, app to PostgreSQL/Redis)
- Testing approach (manual Telegram test, database verification)
</output>
